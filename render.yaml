services:
  # Service web Next.js
  - type: web
    name: pr-2026-bj
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: node scripts/start-render.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_TELEMETRY_DISABLED
        value: "1"
      - key: RATE_LIMIT_MAX_REQUESTS
        value: 100
      - key: RATE_LIMIT_WINDOW_MS
        value: 60000
      - key: DATABASE_URL
        fromDatabase:
          name: pr-2026-db
          property: connectionString
      # NOTE: NEXT_PUBLIC_APP_URL doit être configuré manuellement après le déploiement
      # avec l'URL de votre service (ex: https://pr-2026-bj.onrender.com)

databases:
  # Base de données PostgreSQL
  - name: pr-2026-db
    plan: basic-1gb
    databaseName: pr2026_db
    user: pr2026_user

